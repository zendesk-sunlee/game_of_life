<svg viewBox="0 0 1000 1000"
     xmlns="http://www.w3.org/2000/svg">
    <% game.each do |cell| %>
      <rect width="15px" height="15px" x="<%= cell.x*15 %>" y="<%= cell.y*15 %>" fill="red"/>
    <% end %>
</svg>
